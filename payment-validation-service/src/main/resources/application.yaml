spring:
  application:
    name: payment-validation-service

server:
  port: 8081

payment:
  processing-service-url: http://localhost:8082

management:
  endpoints:
    web:
      exposure:
        include: health
