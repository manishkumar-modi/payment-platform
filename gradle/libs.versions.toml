[versions]
spring-boot = "4.1.1"
spring-dependency-management = "1.1.7"

[libraries]
# Spring MVC runtime for REST controllers and HTTP endpoints.
spring-boot-starter-webmvc = { module = "org.springframework.boot:spring-boot-starter-webmvc" }
# Bean validation annotations and runtime validation support.
spring-boot-starter-validation = { module = "org.springframework.boot:spring-boot-starter-validation" }
# Health, metrics, and operational monitoring endpoints.
spring-boot-starter-actuator = { module = "org.springframework.boot:spring-boot-starter-actuator" }
# Spring Boot test support for service and web-layer tests.
spring-boot-starter-webmvc-test = { module = "org.springframework.boot:spring-boot-starter-webmvc-test" }
# Development-time restart and live reload support.
spring-boot-devtools = { module = "org.springframework.boot:spring-boot-devtools" }
# Compile-time annotations for reducing Java boilerplate.
lombok = { module = "org.projectlombok:lombok" }
# JUnit Platform launcher used to discover and run tests.
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher" }

[plugins]
# Spring Boot application packaging and execution tasks.
spring-boot = { id = "org.springframework.boot", version.ref = "spring-boot" }
# Centralizes dependency versions managed by Spring Boot.
spring-dependency-management = { id = "io.spring.dependency-management", version.ref = "spring-dependency-management" }
